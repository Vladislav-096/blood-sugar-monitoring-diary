import { DiaryData } from "../../features/diaryData/DiaryData";

export const TablePage = () => {
  return (
    <section>
      <div className="container">
        <DiaryData />
      </div>
    </section>
  );
};
